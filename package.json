{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "prepublish": "lerna run build",
    "publish": "lerna publish",
    "format": "prettier --write packages/**/*.ts",
    "format-check": "prettier --check packages/**/*.ts",
    "test": "jest --testTimeout 10000",
    "test-one": "jest --testTimeout 10000 packages/step"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/jest": "^28.1.6",
    "@types/js-yaml": "^4.0.5",
    "jest": "^28.1.3",
    "lerna": "^5.3.0",
    "prettier": "^2.7.1",
    "ts-jest": "^28.0.7",
    "typescript": "^4.7.4"
  }
}
