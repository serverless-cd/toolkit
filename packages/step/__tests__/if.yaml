name: "On Push master"
on:
  push:
    branches:
      - master
job:
  runtime: nodejs12
  env:
    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    FIRST_NAME: Mona
    LAST_NAME: Octocat
  steps:
    - run: npm run xxx
    - run: echo "hello"
    - run: echo "world"
      if: ${{ failure() }}
    - run: echo "end"
      if: ${{ success() }}