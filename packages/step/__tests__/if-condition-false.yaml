name: "On Push master"
on:
  push:
    branches:
      - master
job:
  runtime: nodejs12
  env:
    FIRST_NAME: Mona
    LAST_NAME: Octocat
  steps:
    - run: echo "hello"
      id: xhello
    - run: echo "world"
      if: '{{ steps.xhello.output.code !== 0 }}'
      id: xworld
    - run: echo "end"
      id: xend
