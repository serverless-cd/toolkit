name: "On Push master"
on:
  push:
    branches:
      - master
job:
  runtime: nodejs12
  env:
    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    FIRST_NAME: Mona
    LAST_NAME: Octocat
  steps:
    - name: 'list files'
      run: |
        aa
      working-directory: ./
    - run: |
        npm install
      working-directory: ./
    - run: |
        npm --version
      working-directory: ./