{
  "name": "@serverless-cd/webhook",
  "version": "0.0.1-alpha.6",
  "private": true,
  "description": "sevrerless-cd webhook",
  "main": "./lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "tsc -w",
    "watch": "esbuild src/index.ts --bundle --log-level=error --minify --platform=node --format=cjs --target=node10.4 --external:@serverless-devs/core --outfile=example/lib/index.js --watch",
    "esbuild": "rm -rf lib && esbuild src/index.ts --bundle --log-level=error --minify --platform=node --format=cjs --target=node10.4 --external:@serverless-devs/core --outfile=lib/index.js",
    "postesbuild": "rm -rf example/lib && cp -rf lib example/lib"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bl": "^5.0.0",
    "crypto": "^1.0.1"
  },
  "devDependencies": {
    "@types/bl": "^5.0.2",
    "@types/node": "^18.7.1",
    "esbuild": "^0.14.54"
  },
  "publishConfig": {
    "access": "public"
  }
}
